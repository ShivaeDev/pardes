interface:
  display_name: "Implement Change"
  short_description: "Delegate an approved PR to managed workers"
  default_prompt: "Use $implement-change to delegate this approved PR to an owner worker."
policy:
  allow_implicit_invocation: true
