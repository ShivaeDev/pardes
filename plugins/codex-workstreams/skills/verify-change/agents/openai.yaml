interface:
  display_name: "Verify Change"
  short_description: "Coordinate delegated review and quality gates"
  default_prompt: "Use $verify-change to decide whether this branch is ready for a PR."
policy:
  allow_implicit_invocation: true
